<p id="notice"><%= notice %></p>

<h1>Flights</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Flight number</th>
      <th>Origin</th>
      <th>Destination</th>
      <th>Flight date</th>
      <th>Airplane</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @flights.each do |flight| %>
      <tr>
        <td><%= flight.flight_number %></td>
        <td><%= flight.origin %></td>
        <td><%= flight.destination %></td>
        <td><%= flight.flight_date %></td>
        <td><%= flight.airplane_id %></td>
        <td><%= link_to 'Show', flight %></td>
        <td><%= link_to 'Edit', edit_flight_path(flight) %></td>
        <td><%= link_to 'Destroy', flight, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Flight', new_flight_path, class: "btn btn-primary" %>
<%= link_to 'Search Flights', search_path, class: "btn btn-primary" %>
<!--

<button id="search-button" class="btn btn-default">Search Flights</button>

<div id="app"></div>

<script type="html/template" id="SearchViewTemplate">
  <input type="text" id="from" placeholder="from">
  <input type="text" id="to" placeholder="to">
  <button class="btn btn-default" id="search-flights">Search</button>

  <h2>Flight search results</h2>
  <ul id="search-results"></ul>
</script> -->
